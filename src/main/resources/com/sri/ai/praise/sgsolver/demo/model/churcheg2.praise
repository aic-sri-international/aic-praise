@LANG:Church
@MODEL:name=[Page 1] parts=[page-1-model] queries=[query#1,query#2,query#3]
@FRAGMENT:page-1-model
;
; Church Example 2
;
(query
  ; -- generative-model -- 
  (define A (flip))
  (define B (flip))
  (define C (flip))
  ; -- what-we-want-to-know --
  A
  ; -- what-we-know --
  (or (and A B) (and A C) (and B C)) 
)
@FRAGMENT:query#1
a

@FRAGMENT:query#2
b

@FRAGMENT:query#3
c
