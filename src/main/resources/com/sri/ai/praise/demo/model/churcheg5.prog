;
; Church Example 5
;
(query
  ; -- generative-model -- 
  (define a (flip))
  (define b (flip))
  (define c (flip))
  ; -- what-we-want-to-know --
  a
  ; -- what-we-know --
  (or (and a b) (and a c) (and b c)) 
)