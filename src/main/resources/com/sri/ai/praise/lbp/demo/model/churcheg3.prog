;
; Church Example 3
;
(query
  ; -- generative-model -- 
  (define goOut (mem (lambda (day) (if (eq? day 'friday) (flip 0.8) (flip 0.3)))))
  ; -- what-we-want-to-know --
  (goOut 'friday)
  ; -- what-we-know -- 
)